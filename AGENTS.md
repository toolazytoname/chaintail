# Agents

This repo is a **read-only** local chain tail.

- One chain in v0.1. Do not start a ten-chain abstraction.
- No keys, no broadcast, no full-node.
- Tests must run without mainnet (fixtures).
- Spec: `docs/PROJECT-PLAN.md`. Security: `SECURITY.md`.
